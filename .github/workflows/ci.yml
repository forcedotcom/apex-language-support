name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    name: Test
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: ['20.x', 'lts/*', 'node']
      fail-fast: false

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Compile project
        run: npm run compile

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Merge coverage reports
        run: npm run test:coverage:report

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-${{ matrix.os }}-${{matrix.node-version == 'lts/*' && 'lts' ? 'lts' : matrix.node-version}}
          path: ./coverage

  # slack-notify:
  #   name: Slack Notification
  #   needs: [test]
  #   runs-on: ubuntu-latest
  #   if: always() && github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Notify Slack
  #       uses: salesforcecli/github-workflows/.github/actions/slack-notify@main
  #       with:
  #         channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
  #         slack-token: ${{ secrets.IDEE_MAIN_SLACK_WEBHOOK }}
  #         status: ${{ needs.test.result }}
