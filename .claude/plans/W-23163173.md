# W-23163173 — 1.2 promote queueState/graphData types to shared

## Context

- Epic: Apex LSP Client SDK (`a3QEE000002UYr72AG`). Parallel group 1 (sibling of 1.1/1.3/1.4); independent.
- add `QueueStateParams/Result` + `GraphDataParams/Result` to `apex-lsp-shared`. Non-breaking promotion. Type-level assert current local shapes assignable to shared.
- Loose shapes today (source of truth):
  - `packages/lsp-compliant-services/src/services/QueueStateProcessingService.ts` — `QueueStateParams`, `QueueStateResponse`
  - `packages/lsp-compliant-services/src/services/GraphDataProcessingService.ts` — `GraphDataParams`, `GraphDataResponse`
  - both re-exported from `packages/lsp-compliant-services/src/index.ts`
- `apex-lsp-shared` deps = effect, zod, vscode-jsonrpc, vscode-languageserver, otel. NO `apex-parser-ast` dep (intentional; index.ts:120 "keep shared lightweight"). Shared types must STRUCTURALLY model parser-ast shapes, not import them.
- Reusable from shared: `Priority` (`types/priority`), `EnumValue` (`enumUtils`), `Diagnostic` (vscode-languageserver).
- Module placement: `apex-lsp-shared` has NO `protocol/` dir and no `protocol/*` export pattern. Root-level type/schema modules live directly in `src/*.ts` (`types.ts`, `wireSchemas.ts`, `workerWireSchemas.ts`) and are re-exported from `index.ts` via named export blocks (see `index.ts:413-506` for `wireSchemas`/`workerWireSchemas`). New protocol types follow THAT pattern: a root `src/queueStateGraphData.ts`, exported with an explicit named `export type { … }` block. Do NOT invent a `protocol/` directory.
- Shapes to model (refs only):
  - `SchedulerMetrics` — `apex-parser-ast/src/types/queue.ts:40` (uses `Priority` records)
  - `GraphData`/`GraphNode`/`GraphEdge`/`FileGraphData`/`TypeGraphData` — `apex-parser-ast/src/types/graph.ts`
  - `SymbolLocation`/`SymbolModifiers` — `symbol.ts:451,490`; `ReferenceType` — `ApexSymbolRefManager.ts:101` (const obj → `EnumValue`)
  - `DiagnosticGraphCorrelation` — `lsp-compliant-services/src/types/diagnosticGraph.ts:34`
- WI says `*Params/Result`. Local uses `*Params/*Response`. Add shared as `QueueStateParams/QueueStateResult` + `GraphDataParams/GraphDataResult`; assert local `*Response` assignable to shared `*Result`.

## Phases

### Phase 1 — shared types module

- `packages/apex-lsp-shared/src/queueStateGraphData.ts` (root-level module, NOT a `protocol/` subdir — that dir does not exist; mirror `wireSchemas.ts`/`workerWireSchemas.ts` placement):
  - `SchedulerMetricsShape` (Priority-keyed records, optional breakdowns, `workerTopology`)
  - graph types: `GraphNodeShape`, `GraphEdgeShape` (`type: EnumValue<...>`→model as `string|number`), `GraphDataShape`, `FileGraphDataShape`, `TypeGraphDataShape`
  - `DiagnosticGraphCorrelationShape` (uses vscode `Diagnostic`)
  - `QueueStateParams`, `QueueStateResult`, `GraphDataParams`, `GraphDataResult`
  - structural-clone-safe (no classes/effects); doc fields
- export from `packages/apex-lsp-shared/src/index.ts` via an explicit named `export type { … }` block (mirror the `wireSchemas`/`workerWireSchemas` re-export blocks at `index.ts:413-506`); add a `// QueueState/GraphData protocol types` section comment alongside them. These are type-only, so use `export type { … }`.
- verify: `npm run compile -w @salesforce/apex-lsp-shared`.
- commit: `feat(apex-lsp-shared): add QueueState/GraphData protocol types - W-23163173`

### Phase 2 — type-level assignability assertion + wire local shapes

- assertion in `lsp-compliant-services` (needs both shared + parser-ast visibility; shared can't import parser-ast).
- `packages/lsp-compliant-services/test/protocol/sharedTypesAssignable.test.ts` (compile-time):
  - `expectAssignable` / `satisfies`: local `QueueStateParams`→shared `QueueStateParams`; local `QueueStateResponse`→shared `QueueStateResult`; same for `GraphData*`.
  - assert both directions where non-breaking requires (local assignable to shared at minimum).
- optional: re-export shared types from `lsp-compliant-services/src/index.ts` for downstream; keep local as canonical-runtime, shared as protocol contract. Keep service signatures unchanged.
- verify: `npm test -w @salesforce/lsp-compliant-services`; `npm run compile -w @salesforce/lsp-compliant-services`.
- commit: `test(lsp-compliant-services): assert local queue/graph shapes assignable to shared - W-23163173`

## Skills to apply

- typescript — file-naming, type conventions
- concise — plan/doc style
- writing-tests — type-assertion test
- packageJson / wireit — only if exports/build wiring touched
- verification — pre-merge checklist

## Verification

- compile apex-lsp-shared; no parser-ast import.
- module lives at `src/queueStateGraphData.ts` (root); confirm no `src/protocol/` dir was created (`ls packages/apex-lsp-shared/src` shows no `protocol/`).
- `index.ts` re-export uses a named `export type { … }` block matching the `wireSchemas`/`workerWireSchemas` style; new symbols resolve from `@salesforce/apex-lsp-shared` root.
- test lsp-compliant-services; assignability test passes.
- compile lsp-compliant-services; local shapes still build.
- grep confirm apex-lsp-shared/package.json deps unchanged (no parser-ast added).
- lint clean on touched files.
- NOT e2e-covered: pure type-level promotion; no runtime/protocol behavior change → no e2e on branch exercises this. Compile + type-assertion test are the gate.
