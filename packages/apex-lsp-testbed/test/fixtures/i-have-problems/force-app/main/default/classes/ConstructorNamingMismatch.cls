/**
 * Demonstrates: Constructor name doesn't match class name
 * Error Code: invalid.constructor.name
 * Validator: ConstructorNamingValidator (TIER 1)
 */
public class ConstructorNamingMismatch {
    public WrongName() {
        // Constructor name doesn't match class name - error!
    }
}
