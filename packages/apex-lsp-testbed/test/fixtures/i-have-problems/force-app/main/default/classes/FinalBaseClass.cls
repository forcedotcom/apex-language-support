/**
 * Base class for ClassHierarchyIssue demonstration
 * This class is final by default (cannot be extended) since it's not marked as virtual.
 * In Apex, classes are final by default - you cannot use the 'final' keyword on classes.
 * To make a class extensible, use the 'virtual' keyword instead.
 */
public class FinalBaseClass {
    public void baseMethod() {
    }
}
