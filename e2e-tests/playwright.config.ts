/**
 * Main Playwright configuration file.
 * 
 * Re-exports configuration from the config directory to maintain
 * backward compatibility while improving organization.
 */

export { default } from './config/playwright.config';