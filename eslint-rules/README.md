# Custom ESLint Rules

This directory contains custom ESLint rules specific to this project.

Currently, there are no active custom ESLint rules. Previously, this directory contained Turbo-specific rules that have been removed as part of the migration to Wireit.
